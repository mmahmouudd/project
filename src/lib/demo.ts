/**
 * Demo credentials shown on the sign-in screen.
 * Kept in a dependency-free module so client components can import it
 * without dragging the database driver into the browser bundle.
 */
export const DEMO_EMAIL = "juniper@willowlane.school";
export const DEMO_PASSWORD = "sage-board";
